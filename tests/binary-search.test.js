const { getData, indexOfBinarySearch } = require('../logic/binary-search');
const sort = require('../logic/sort');

const directory = getData();
const length = directory.length;
const x = 5;

test('Test First Index Of Binary Search - Success', () => {
    // const input = sort.defaultSort(directory);
    // const output = indexOfBinarySearch(directory, 'Jonathan', length - 1);
    // expect(output).toBe(0);
});